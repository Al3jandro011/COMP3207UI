/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['example.com', 'img.freepik.com'], // Add any other domains you're using for images
  },
};

export default nextConfig;
