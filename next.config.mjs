/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.example.com', 'img.freepik.com', 'example.com', 'media.istockphoto.com'],
  },
};

export default nextConfig;
